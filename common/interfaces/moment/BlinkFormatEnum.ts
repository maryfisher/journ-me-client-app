module jm.moment {
    export enum BlinkFormatEnum {
        right_image,
        left_image,
        double_text,
        single_text,
        video,
        single_image,
        double_image
    }
}